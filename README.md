# SimplestColabDocs

Una herramienta simple de documentación colaborativa para sitios estáticos, mantenida usando Git.

A simple documentation collaboration tool for static sites, maintained using Git.

## Filosofía / Philosophy

- Sin librerías externas de JavaScript para evitar vulnerabilidades de seguridad y tener control absoluto.
- No external JavaScript libraries to prevent security vulnerabilities and have absolute control.

- Estructura en árbol para permitir temas y subtemas.
- Tree-like structure to allow themes and subthemes.

- Soporte completo de Markdown incluyendo imágenes, bloques de código, tablas y diagramas.
- Full Markdown support including images, code blocks, tables, and diagrams.

- Colaboración mediante Pull Requests de Git: un nuevo documento debe subirse al repositorio como .md y se reflejará en el sitio.
- Collaboration through Git Pull Requests: a new document should be uploaded to the repository as .md and it will be reflected on the site.

- Tema claro y oscuro.
- Light and Dark theme.

- Barra de búsqueda para buscar en todo el texto.
- Search bar to search all text.

- Internacionalización: Español e Inglés (Español por defecto).
- Internationalization: Spanish and English (Spanish as default).

## Stack Tecnológico / Tech Stack

HTML - CSS - JavaScript (Vanilla)

## Cómo Agregar un Documento / How to Add a Document

1. Añade tu archivo `.md` en la carpeta `docs` (o una subcarpeta)
   Add your `.md` file in the `docs` folder (or a subfolder)

2. Si tu documento contiene imágenes, colócalas en una carpeta con el mismo nombre que tu documento (sin .md)
   If your doc contains images, place them in a folder with the same name as your doc (without .md)

3. **Push a la rama principal** - La GitHub Action actualizará automáticamente `docs.json` con tu nuevo documento
   **Push to main branch** - The GitHub Action will automatically update `docs.json` with your new document

El archivo `docs.json` se genera automáticamente por una GitHub Action cuando haces push de cambios a la carpeta `docs/`. Incluye:
The `docs.json` file is automatically generated by a GitHub Action when you push changes to the `docs/` folder. It includes:

- Rutas de documentos / Document paths
- Fecha de agregado (del historial de git) / Date added (from git history)
- Los documentos se ordenan por fecha (más reciente primero) en la pestaña "Todos los Artículos"
- Documents are sorted by date (most recent first) in the "All Articles" tab

### Formato manual de docs.json (si es necesario) / Manual docs.json format (if needed):

```json
{
    "Category Name": {
        "YourDocument.md": {
            "path": "docs/YourDocument.md",
            "date": "2025-12-05"
        }
    }
}
```

Para carpetas anidadas / For nested folders:

```json
{
    "Category": {
        "Subcategory": {
            "Document.md": {
                "path": "docs/Category/Subcategory/Document.md",
                "date": "2025-12-05"
            }
        }
    }
}
```

## Características / Features

- 🌐 Internacionalización ES/EN (Español por defecto) / Internationalization ES/EN (Spanish as default)
- 🔍 Búsqueda de texto completo / Full-text search
- 🌓 Tema claro/oscuro / Light/dark theme
- 📱 Diseño responsive / Responsive design
- 📝 Soporte completo de Markdown / Full Markdown support
- 🔗 Enlaces internos entre documentos / Internal links between documents
- 📚 Historial de lectura / Reading history
- 🎯 Sin dependencias externas / No external dependencies

<img width="32" height="32" alt="the guy" src="https://github.com/user-attachments/assets/1bcb93fb-c861-4457-ad31-9b1a5b897842" />

